language: generic 
sudo: required
services:
    - docker

before_install:
    - docker build -t emanuelemoretta/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true emanuelemoretta/docker-react npm run test -- --coverage